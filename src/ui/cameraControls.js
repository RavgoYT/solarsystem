import { planetData } from '../planets/planetData.js';

export function setupCameraControls(orbs, orbitLines, camera, controls) {
    // No-op: all camera transitions and keyboard logic are handled in script.js
    // Ill move them here later, I just couldn't figure out the global variable for it.
}

export function updateCamera(deltaTime, camera, controls, orbs, currentOrbIndex) {
    // No-op: camera transitions handled in script.js
}